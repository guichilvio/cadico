<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proyectos - CADICO</title>
    
</head>
<body>

    <section class="proyectos">
        <div class="contenido-proyectos">
            <h2>Proyectos</h2>
            <img src="PT 2.jpg" alt="Proyecto Principal" class="imagen-principal" onclick="openModal(this)">
            <div class="contenedor-miniaturas">
                <img src="PT 1.jpg" alt="Interior 1" class="miniatura" onclick="openModal(this)">
                <img src="PT 3.jpg" alt="Interior 2" class="miniatura" onclick="openModal(this)">
                <img src="PT 4.jpg" alt="Interior 3" class="miniatura" onclick="openModal(this)">
                <img src="PT 5.jpg" alt="Interior 4" class="miniatura" onclick="openModal(this)">
            </div>
        </div>
    </section>

    <div id="modal" class="modal" style="display: none;">
        <span class="close" onclick="closeModal()">&times;</span>
        <img id="modal-img">
    </div>

    <script type="text/javascript">
        function openModal(imgElement) {
            document.getElementById('modal-img').src = imgElement.src;
            document.getElementById('modal').style.display = 'flex';
        }

        function closeModal() {
            document.getElementById('modal').style.display = 'none';
        }
    </script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Century Gothic', sans-serif;
        }

        body {
            background: #f8f7f9;
            text-align: center;
            padding: 50px 20px;
        }

        .proyectos {
            position: relative;
            width: 100%;
            min-height: 100vh;
            background: url('hero_2.jpg') no-repeat center center/cover;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 80px 0;
        }

        .contenido-proyectos {
            background: rgba(255, 255, 255, 0.9);
            padding: 60px;
            border-radius: 20px;
            width: 85%;
            max-width: 1100px;
            text-align: center;
            color: #333;
            box-shadow: 0px 10px 25px rgba(0, 0, 0, 0.3);
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .contenido-proyectos h2 {
            font-size: 42px;
            font-weight: bold;
            margin-bottom: 25px;
            color: #cfa52f;
            font-family: 'Baskerville Old Face', serif;
        }

        .imagen-principal {
            width: 100%;
            max-width: 900px;
            border-radius: 15px;
            margin: 25px auto;
            box-shadow: 0px 12px 24px rgba(0, 0, 0, 0.4);
            display: block;
            transition: transform 0.4s ease-in-out;
        }

        .imagen-principal:hover {
            transform: scale(1.05);
        }

        .contenedor-miniaturas {
            display: flex;
            justify-content: center;
            gap: 25px;
            flex-wrap: wrap;
            margin-top: 20px;
        }

        .miniatura {
            width: 18%;
            max-width: 160px;
            border-radius: 15px;
            cursor: pointer;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            box-shadow: 0px 6px 14px rgba(0, 0, 0, 0.3);
        }

        .miniatura:hover {
            transform: scale(1.1);
            box-shadow: 0px 8px 18px rgba(0, 0, 0, 0.5);
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            align-items: center;
            justify-content: center;
        }

        .modal img {
            max-width: 80%;
            max-height: 80%;
            border-radius: 10px;
            box-shadow: 0px 8px 16px rgba(255, 255, 255, 0.3);
        }

        .close {
            position: absolute;
            top: 20px;
            right: 30px;
            color: white;
            font-size: 40px;
            font-weight: bold;
            cursor: pointer;
        }

        @media (max-width: 900px) {
            .proyectos {
                height: auto;
                padding: 50px 0;
            }

            .contenido-proyectos {
                width: 90%;
                padding: 35px;
            }

            .imagen-principal {
                width: 100%;
            }

            .miniatura {
                width: 30%;
            }
        }
    </style>
    

</body>
</html>
